namespace CRM.Core.Constants.Logs.DataLayer;

public static class LeadsRepositoryLogs
{
    public const string AddLead = "Adding to the database lead with ID {id}";
    public const string GetLeadById = "Search lead with ID {id} at the database";
    public const string GetLeadByMail = "Search lead with mail {mail} at the database";
    public const string UpdateLead = "Update lead with ID {id} at the database";
}
